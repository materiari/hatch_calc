%h1= "Week #{@week}"
-@days.each do |day| 
  %h2= "Day #{day}"
  %table.workout{:id => "day_#{day}"}
    %thead
      %tr
        %th.lift{:colspan => 3} Back Squat 
        %th.lift{:colspan => 3} Front Squat 
      %tr
        %th.scheme reps
        %th.scheme %1RM
        %th.scheme Weight 
        %th.scheme reps
        %th.scheme %1RM
        %th.scheme Weight 
    %tbody
      -@workout[day].each do |set| 
        %tr
          %td= set.first.reps
          %td= set.first.percentage 
          %td= set.first.weight.round(2)
          -if set.last.nil? 
            %td/
            %td/
            %td/
          -else
            %td= set.last.reps
            %td= set.last.percentage 
            %td= set.last.weight.round(2)
          
